version: "3.9"

services:

  archivospublicos:
    build:
      context: .
      dockerfile: Dockerfile
    image: archivospublicos:0.0.1
    container_name: archivospublicos
    volumes:
      - .:/srv/app
      - /srv/app/node_modules
      - /srv/app/frontend/bower_components
    ports:
      - "2201:80"